services:
  - type: web
    name: SINTRAMDIB-1
    env: node
    region: ohio
    plan: free
    rootDir: apps/api
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm start